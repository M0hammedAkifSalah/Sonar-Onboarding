import { UploadBtnPipe } from './upload-btn.pipe';

describe('UploadBtnPipe', () => {
  it('create an instance', () => {
    const pipe = new UploadBtnPipe();
    expect(pipe).toBeTruthy();
  });
});
